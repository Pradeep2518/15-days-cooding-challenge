class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        n=len(nums)
        c=0
        m=float('-inf')
        for i in range(n):
            c=c+nums[i]
            m=max(m,c)
            if c<0:
                c=0
        return m

        
